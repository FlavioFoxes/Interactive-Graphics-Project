{
  "dependencies": {
    "cannon-es": "^0.20.0",
    "cannon-es-debugger": "^1.0.0",
    "three": "^0.174.0",
    "three-csg-ts": "^3.2.0",
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "@types/three": "^0.174.0"
  },
  "scripts": {
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
